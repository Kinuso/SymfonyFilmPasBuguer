{% extends 'base.html.twig' %}

{% block title %}Hello FilmController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
<h1>Voici la liste de tout les films : </h1>
<div class="example-wrapper">

{% for message in app.flashes('success') %}
    
    <p> {{message | join('<br>')}} </p>

{% endfor %}

{% for films , titre in film %}
    <a href="{{path('app_film_show' , { "titre" : film[films].titre})}}">Voir {{film[films].titre}} </a><br>
    <form action="{{path('app_film_delete' , {id :film[films].id}) }}" method="POST">
    <input type="hidden" name="_method" value="DELETE">
    <input type="submit" name="" value="supprimer ce film"id=""></form>
{% endfor %}


<br>
<br>
<a href="{{path('app_film_new')}}">Vers la création de film (normalement)</a>

</div>
{% endblock %}
